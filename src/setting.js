let Setting = {
  /*
    //method:http请求方法get post
    setting.ajaxHandler = function(method,url,param,callback){
      .......
      //success
      callback(null,data);
      //error
      callback(err);
    };
  */
  ajaxHandler : null
};
export default Setting;